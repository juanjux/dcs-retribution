*.pyc
__pycache__
build/**
# Sphinx
docs/_build
resources/payloads/*.lua
venv
.DS_Store
.vscode/settings.json
dist/**
/.coverage
/coverage.xml
# User-specific stuff
.idea/
.env
env/

/kneeboards
/liberation_preferences.json
/state.json
/serverconfig.env

/logs/
/resources/logging.yaml
/resources/plugins/pretense/pretense_output.lua

*.psd

# Local virtualenv (matches existing `venv` convention but with leading dot)
.venv/

# Local fork build output and scratch files. `dist/**` above only covers the stock
# build dir; the buffed-fork build script writes elsewhere, and a stray `git add -A`
# once swept 8k build files (incl. a 147 MB Qt DLL) into a commit that GitHub then
# refused to accept.
/dist_full_fork/
/build_full_fork/
/_modtools/
/_design_review/
/_rebuild_*.log
/state.json.*_bak
