{
    "nameInUI": "Lua tick profiler (development)",
    "descriptionInUI": "Loads LuaTickProfile so a plugin can measure what one of its ticks costs: VM instructions per tick, the share of real time it takes, and how much it allocates. Nothing is measured until a script asks for it, and the count hook it uses is not free -- switch this on for a measurement run, not for a campaign. Off by default.",
    "defaultValue": false,
    "specificOptions": [
        {
            "nameInUI": "Seconds between reports",
            "mnemonic": "window",
            "defaultValue": 60,
            "minimumValue": 5,
            "maximumValue": 600
        }
    ],
    "scriptsWorkOrders": [
        {
            "file": "tickprofile.lua",
            "mnemonic": "tickprofile"
        }
    ],
    "configurationWorkOrders": []
}
